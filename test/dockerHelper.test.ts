import * as utils from '../src/utils'
import {expect, test} from '@jest/globals'
import * as docker from '../src/dockerHelper'

 test('check docker install download', async() => {
 //   expect(await docker.parseDockerVersion("Docker version 20.10.14, build a224086")).toEqual("20.10.14");

//    expect(await docker.getVersion()).toEqual("20.10.14");

//    expect(await docker.checkDockerInstall()).toEqual(true);

//    expect(await docker.checkDockerSuitable()).toEqual(true);
})